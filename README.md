# 📈 Биржабот

Интеллектуальный бот для анализа исторических котировок и прогнозирования цен акций на 30 дней вперёд с использованием ансамбля моделей ML и рекомендациями по торговле.

## 📋 Требования

- **Python** 3.9 или выше
- **Интернет-соединение** для загрузки данных через Yahoo Finance
- **Токен Telegram-бота** (получить у [@BotFather](https://t.me/BotFather))

## ✨ Функциональность

- ✅ **Ввод тикера и суммы инвестиции** — анализ любых ценных бумаг с Yahoo Finance
- ✅ **Автоматический выбор лучшей модели** (RandomForest / Prophet / LSTM) на основе метрики MAPE
- ✅ **Интерактивные графики** — история цен (90 дней) + прогноз (30 дней)
- ✅ **Торговые сигналы** — точки входа/выхода и расчёт ожидаемой прибыли
- ✅ **Логирование запросов** — все операции записываются в `logs/logs.csv`

## 📁 Структура проекта

```
stock_bot_project_final/
├── run.py                  # Точка входа
├── requirements.txt        # Зависимости проекта
├── README.md              # Этот файл
│
├── bot/                   # Telegram-бот и его логика
│   ├── __init__.py
│   └── app.py            # Обработчики команд и сообщений
│
├── data/                  # Загрузка и обработка данных
│   ├── __init__.py
│   └── loader.py         # Загрузка исторических котировок с Yahoo Finance
│
├── models/                # Модели прогнозирования
│   ├── __init__.py
│   ├── lstm.py           # Двухслойная нейросеть с дропаутом
│   ├── prophet.py        # Статистическая модель Meta
│   ├── random_forest.py  # Лес с 10 лаговыми признаками
│   └── model_selector.py # Выбор по метрике MAPE
│
├── utils/                 # Вспомогательные функции
│   ├── __init__.py
│   ├── logger.py         # Логирование в logs.csv
│   ├── plotting.py       # Генерация графиков
│   └── trading.py        # Вычисление торговых сигналов и прибыли
│
├── logs/                  # Логирование операций
│   ├── __init__.py
│   └── logs.csv          # История всех запросов пользователей
│
└── tests/                 # Интеграционные тесты
    ├── __init__.py
    ├── test_loader.py           # Тест загрузки данных
    ├── test_lstm_model.py       # Тест LSTM нейросети
    ├── test_prophet_model.py    # Тест Prophet модели
    ├── test_random_forest.py    # Тест Random Forest модели
    ├── test_model_selection.py  # Тест выбора лучшей модели
    ├── test_trading_strategy.py # Тест торговых сигналов
    └── test_plotting.py         # Тест визуализации
```

## 🚀 Установка и запуск

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd stock_bot_project_final
```

### Шаг 2: Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate        # macOS / Linux
# или
venv\Scripts\activate           # Windows
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Или создайте его вручную и добавьте ваш токен:

```env
BOT_TOKEN=your_telegram_bot_token_here
```

Получить токен можно у [@BotFather](https://t.me/BotFather) в Telegram.

### Шаг 5: Запуск бота

```bash
python run.py
```

Бот начнёт прослушивать входящие сообщения в Telegram.

## 💬 Использование в Telegram

1. **Найдите бота** в Telegram по имени (или добавьте к приватным чатам)
2. **Отправьте команду** `/start` — бот поприветствует вас
3. **Введите тикер** акции (например: `AAPL`, `MSFT`, `TSLA`, `GAZP`)
4. **Укажите сумму инвестиции** в USD (например: `1000`)
5. **Получите результаты:**
   - 📊 **График** с историей цен (90 дней) и прогнозом (30 дней)
   - 📈 **Прогнозная цена** на 30 дней вперёд
   - 💡 **Торговые сигналы** — точки входа (BUY) и выхода (SELL)
   - 💰 **Расчёт ожидаемой прибыли** при использовании стратегии

## 🤖 Модели прогнозирования

Бот тренирует **три модели** на исторических данных и автоматически выбирает лучшую по метрике **MAPE** (Mean Absolute Percentage Error):

| Модель | Описание | Параметры |
|--------|---------|-----------|
| **RandomForest** | Ensemble метод | 100 деревьев, 10 лаговых признаков |
| **Prophet** | Статистическая модель | Сезонность (годовая, недельная) |
| **LSTM** | Рекуррентная нейросеть | 2 слоя, dropout=0.2, 32 юнита |

Лучшая модель выбирается автоматически и используется для финального прогноза.

## ⚠️ Важные замечания

> ⚠️ **Это учебный проект** и не должен использоваться как финансовая рекомендация.

- 🤖 **Бот не совершает реальные сделки** — только анализирует и предлагает рекомендации
- 📊 **Прогнозы основаны на исторических данных** и могут быть неточными
- 📉 **Качество прогнозов зависит от волатильности рынка** и стабильности котировок
- 💼 **Используйте на свой риск** и не доверяйте 100% автоматическим рекомендациям
- 🔐 **Никогда не делитесь токеном бота** и приватной информацией